{
  "name": "nestforge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo do NestForge — CLI (create-nestforge) e templates de boilerplate NestJS",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
