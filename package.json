{
  "name": "nestforge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "NestForge monorepo — CLI and production-ready NestJS starter templates",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
